
const TitleH2 = (props) => {
    
    return (
        <div className={props.divClass}>
            <h2 className="heading-secondary">{props.title}</h2>
        </div>
    );
};

export default TitleH2;